pre-commit:
  commands:
    validate:
      # run: deno run -A ./git_hooks/validate_staged_files.ts
      run: deno task lint-staged -c ./lintstaged.json

commit-msg:
  commands:
    lint:
      run: deno task commitlint -g ./commitlint.json --edit {1}
